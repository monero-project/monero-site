---
interface Props {
  class?: string;
  id?: string;
  variant?: "spaced" | "flush";
}

const { id, class: className, variant = "spaced", ...rest } = Astro.props;
const classes = [
  variant === "spaced" ? "page-container--spaced" : "",
  className ?? "",
]
  .filter(Boolean)
  .join(" ");
---

<div id={id} class={classes} {...rest}>
  <slot />
</div>

<style>
  .page-container--spaced {
    margin-top: 2rem;
  }
</style>
