<article id="blog-content" lang="en" dir="ltr">
  <slot />
</article>

<style is:global lang="scss">
  #blog-content {
    line-height: 1.8;

    :where(h2, h3, h4) {
      font-weight: 700;
    }

    h2 {
      font-size: var(--font-size-2xl);
    }

    h3 {
      font-size: var(--font-size-xl);
    }

    h4 {
      font-size: var(--font-size-lg);
    }

    p {
      margin-bottom: 1em;
    }

    ul,
    ol {
      margin: 1em 0 1em 1.5em;
      padding-left: 1.25em;
    }

    li {
      margin-bottom: 0.25em;
    }

    pre {
      padding: 1em;
      border-radius: 0.5em;
      overflow-x: auto;
      background: #111;
    }

    pre code {
      background: none;
      padding: 0;
      border-radius: 0;
    }

    code:not(pre code) {
      font-family: monospace;
      background-color: #f0f0f0;
      padding: 0.1em 0.3em;
      border-radius: 0.2em;
    }

    blockquote {
      margin: 1.5em 0;
      padding-left: 1em;
      border-left: 3px solid var(--border-color);
      font-style: italic;
      color: var(--text-secondary);
    }

    @media (prefers-color-scheme: dark) {
      pre {
        background: #111;
      }

      code:not(pre code) {
        background-color: #333;
      }
    }
  }
</style>
