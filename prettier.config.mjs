/** @type {import("prettier").Config} */
export default {
  // Astro formatting
  plugins: ["prettier-plugin-astro"],
  overrides: [
    {
      files: "*.astro",
      options: {
        parser: "astro",
      },
    },
  ],
};
